/**
 * W-1 Stub. Silent-Intelligence-Layer (ETA/Konflikte/
 * Empfehlungen) kommt mit T-3 Sprints.
 */
export default function AiHintsTab() {
  return (
    <div className="p-3 text-xs text-gray-400 italic">
      Keine Hinweise verfügbar.
      <div className="mt-2 text-[10px] text-gray-300">
        ETA-Risiko + Konflikt-Detection landen hier (Sprints T-3).
      </div>
    </div>
  );
}
