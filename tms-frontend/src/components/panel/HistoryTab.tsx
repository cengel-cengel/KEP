/**
 * W-1 Stub. Voller Inhalt kommt in eigenem Sprint W-1.1
 * (Historie/Events-Stream pro Entity).
 */
export default function HistoryTab() {
  return (
    <div className="p-3 text-xs text-gray-400 italic">
      Historie kommt in W-1.1 (Events-Stream + Audit-Log).
    </div>
  );
}
